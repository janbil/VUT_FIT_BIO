Usage: estimation.py [options] sourceimage [MODE]

Options:
  -h, --help  show this help message and exit
  -i          Show intermediate images.

MODE sets filtering for edge recognition for given dataset. Default is BERGDATA
MODE = BERGDATA
    	 SAGEMMSO
     	 SYNTHETIC